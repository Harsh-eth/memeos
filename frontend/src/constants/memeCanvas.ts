/** Inner canvas size (matches meme frame content area). */
export const MEME_PREVIEW_W = 504;
export const MEME_PREVIEW_H = Math.round((MEME_PREVIEW_W * 360) / 560);
export const LANDING_MATRIX_SEED = 99;
